import {
  addRecord,
  removeRecord,
  selectRecord,
  clearSelectedRecord,
} from './records'
import editEntry, { clearEdit } from './edit-entry'
import getEntries from './get-entries'

export default {
  addRecord,
  clearEdit,
  clearSelectedRecord,
  editEntry,
  getEntries,
  selectRecord,
  removeRecord,
}

export {
  addRecord,
  clearEdit,
  clearSelectedRecord,
  editEntry,
  getEntries,
  selectRecord,
  removeRecord,
}
